services:
  - type: web
    name: deepread
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt && python -m prisma generate --schema prisma/schema.prisma
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1
    healthCheckPath: /health
